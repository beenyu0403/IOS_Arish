//
//  SignAddFinal.swift
//  arishpractice
//
//  Created by user on 2023/06/27.
//

import UIKit

class SignAddFinal: UIViewController {
    

    override func viewDidLoad() {
        super.viewDidLoad()
        
    }
   /*
    let eyeButton = UIButton(type: .custom)
    let icon = self.textField.isSecureTextEntry ? (비밀번호 안보일 때 눈 아이콘 UIImage) : (비밀번호 보일 때 눈 아이콘 UIImage)
    eyeButton.setImage(icon, state: .normal)
    eyeButton.setImage(icon, state: .highlighted)
    eyeButton.addTarget(self, #selector(버튼 클릭했을 때 실행될 함수), for: .touchUpInside)
    self.textField.rightView = eyeButton
    */
    
    
}
